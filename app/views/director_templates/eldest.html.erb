<h1> Eldest director </h1>

<% eldest = Director.order( { :dob => :asc }).where.not({ :dob => nil }).at(0) %>
The eldest director in our dataset right now is <%= eldest.name%>, born on <%= eldest.dob.strftime("%B %e, %Y") %>.